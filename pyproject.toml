[project]
name = "my-portfolio-video-automation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
	"fastapi",
	"python-multipart",
	"aiofiles",
	"python-dotenv",
	"requests",
	"google-api-python-client",
	"google-auth-oauthlib",
]

[tool.vercel]
entrypoint = "api:app"